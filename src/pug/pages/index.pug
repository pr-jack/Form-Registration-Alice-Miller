extends ../layout/main

block variables
  - var title = 'Test';
  - var description = 'Test task Mihina';
  - var pageClass = 'index-page';

block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }
